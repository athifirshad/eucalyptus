version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: ${NEON_DSN}
    gen:
      go:
        package: "data"
        out: "internal/data"
        sql_package: "pgx/v5"
        emit_json_tags: true
